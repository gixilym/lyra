import type { Component } from "../utils/types";
import MainContainer from "../components/MainContainer";

function Preferences(): Component {
  return (
    <MainContainer>
      <p className="textf-lg text-white">Preferencias</p>
    </MainContainer>
  );
}

export default Preferences;
